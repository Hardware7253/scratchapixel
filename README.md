# Todo
* Make an edge fn module for just doing the maths
* Barycentric coordinate module maybe (idk that maths is closely coupled to whatever is calling it maybe not)
* Render a cool triangle using these two